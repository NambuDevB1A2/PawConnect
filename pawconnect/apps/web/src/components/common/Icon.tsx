import Image from "next/image";

type IconProps = {
    src: string;
    alt?: string;
}

export default function Icon({ src, alt }: IconProps) {
    return (
        <div>
            
        </div>
    );
}