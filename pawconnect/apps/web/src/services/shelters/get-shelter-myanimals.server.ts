import { fetchServer } from "../fetch/fetch.server";

// export const getMyAnimals = async (params?: {
//     page?: number;
//     limit?: number;
//     status?: string;
// }) => {const {data} = await fetchServer.get("")}